node_modules
.next
out
build
.DS_Store
*.pem
.env*
!.env.example
next-env.d.ts
.vercel
*.tsbuildinfo

.vercel
.env*
.next-build

# Any dist dir used to build while a dev server holds .next
.next-*

# Font files pulled down to convert the wordmark to outlines — reproducible
# by rerunning scripts/wordmark-paths.mjs, so not worth committing.
.fontcache/
